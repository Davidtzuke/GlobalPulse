/**
 * PLACEHOLDER - Earthquake visualization layer for the globe.
 *
 * To be implemented by Globe Engineer.
 * - Renders earthquakes as rings/ripples on globe
 * - Size based on magnitude
 * - Color: amber (#f59e0b / pulse-earthquake)
 */

import React from 'react';

function EarthquakeLayer({ data }) {
  return null; // Renders via globe.gl layers
}

export default EarthquakeLayer;
