/** Flight markers layer for Leaflet map
 *
 * - Shows plane icons rotated to match heading (true_track from OpenSky)
 * - Color coded by altitude
 * - Tooltip: callsign, origin country, altitude, speed
 * - Click: show flight details panel
 *
 * TODO: Globe Engineer - implement with react-leaflet markers
 */
import React from 'react'

export default function FlightLayer() {
  return null // Rendered as map layer, not standalone
}
