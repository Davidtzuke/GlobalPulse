/**
 * PLACEHOLDER - Live feed overlay showing recent events.
 *
 * To be implemented by Dashboard Engineer.
 * - Scrolling feed of latest events from all data sources
 * - Color-coded by event type
 * - Click to focus globe on event location
 */

import React from 'react';

function LiveFeed() {
  return (
    <div className="glass-panel p-3 max-h-64 overflow-y-auto">
      <h3 className="text-xs font-semibold text-gray-400 mb-2">Live Feed</h3>
      {/* Feed items */}
    </div>
  );
}

export default LiveFeed;
