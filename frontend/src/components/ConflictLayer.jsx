/**
 * PLACEHOLDER - Conflict visualization layer for the globe.
 *
 * To be implemented by Globe Engineer.
 * - Renders conflict events as pulsing red markers
 * - Size based on event significance
 * - Color: red (#ef4444 / pulse-conflict)
 */

import React from 'react';

function ConflictLayer({ data }) {
  return null; // Renders via globe.gl layers
}

export default ConflictLayer;
